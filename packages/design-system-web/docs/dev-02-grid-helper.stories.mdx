import { Meta } from '@storybook/addon-docs'
import * as Icons from 'react-icons/go'
import { GoAlert, GoMarkGithub } from 'react-icons/go';

<Meta
  title="01 - Dev Helper/Rows & columns"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>


<geov-heading>Rows & columns helpers</geov-heading>

This page regroups exemples of what can be done with ```<geov-row></geov-row>``` and ```<geov-column></geov-column>```.

<geov-heading h-2>Center an element</geov-heading>

<geov-row geov-style="border: 1px solid red;">
    <div>This element is horizontaly centered inside a row, by default</div>
</geov-row>